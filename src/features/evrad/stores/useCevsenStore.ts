
import { createContentPackStore } from '../../contentpacks/store/createContentPackStore';

// Büyük Cevşen: estimated 100 pages? Default 1.
export const useCevsenStore = createContentPackStore('cevsen-storage', 100);
