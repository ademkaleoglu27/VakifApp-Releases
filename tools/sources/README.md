# Source Files

This directory allows for later replacing the placeholder Risale-i Nur text with actual content.

## How to use
1. Acquire legitimate, licensed text files for the books.
2. Modify `tools/build-content-db.js` to parse your source format instead of generating placeholders.
3. Re-run `node tools/build-content-db.js`.
