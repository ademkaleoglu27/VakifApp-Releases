# Legacy Ingestion Scripts

These files are the ORIGINAL, un-refactored ingestion scripts used as benchmarks for "Zero Regression" verification.

**DO NOT RUN THESE SCRIPTS MANUALLY.**
They are used automatically by `verifyParity.js`.

To verifying parity:
```bash
node tools/ingest/verifyParity.js --target all
```
